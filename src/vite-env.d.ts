/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` from package.json's `version` — see
// src/lib/appVersion.ts for the one place that reads it.
declare const __APP_VERSION__: string
